/*
For this quiz, set the href of the <a> in the first nav item to "#1".

You must use jQuery's attr() method!

Adds a link to a list item. 
*/


var navList;

navList = $( "a" ).first().attr("href", "#1");